# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.ruff_cache/

# Virtual environments
.venv

# direnv
.direnv/

# Source texts (copyrighted material, large binaries)
texts/

# Repo-local dev override of ~/.config/lecturer/lecturer.conf (see
# lecturer.py's _DEV_CONFIG) — personal to this checkout, never committed.
/lecturer.conf

# Working directories created by lecturer contain their own
# self-ignoring .gitignore, so they need no entries here.
 
# Jetbrains
.idea/
